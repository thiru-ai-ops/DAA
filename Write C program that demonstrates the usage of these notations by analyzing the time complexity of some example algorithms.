#include <stdio.h>
int main() {
    int n = 10;
    int sum = 0;
    sum = n * (n + 1) / 2;
    for (int i = 0; i < n; i++) {
        sum += i;
    }
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            sum += i + j;
        }
    }
    return 0;
}
